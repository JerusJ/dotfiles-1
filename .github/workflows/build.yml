name: Build Installers

on: push

jobs:
  linux:
    name: Install Linux
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Run Super-Linter
        run: "./install_linux"