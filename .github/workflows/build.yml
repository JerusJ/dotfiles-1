name: Build Installers

on: push

jobs:
  linux:
    name: Linux
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v2

      - name: Install Make
        run: sudo apt-get install -y make

      - name: Run Installer
        run: make
